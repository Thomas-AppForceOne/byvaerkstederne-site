{% extends 'partials/base.html.twig' %}

{# ============================================================================
   Create-event form (/begivenheder/opret) — the shared event-form surface
   (partials/event_form.html.twig): themed, grouped fields with visible help
   texts and a live event-card preview. Select options come from the
   begivenheder blueprint (data-options@); the POST is handled by the
   event-manager plugin's §8.1 contract handler.
   ============================================================================ #}

{% block body %}
    {% include 'partials/event_form.html.twig' with {
        ef_form_name: 'event-create',
        ef_heading: 'Opret begivenhed',
        ef_intro: 'Udfyld felterne — forhåndsvisningen til højre viser, hvordan kortet kommer til at se ud i <a href="/vaerkstedskalenderen">værkstedskalenderen</a>.'
    } %}
{% endblock %}
