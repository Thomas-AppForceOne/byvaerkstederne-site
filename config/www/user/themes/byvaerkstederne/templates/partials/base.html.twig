<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% if page.title %}{{ page.title }} | {% endif %}{{ site.title }}</title>
    <meta name="description" content="{{ page.header.metadata.description ?: page.summary|striptags|truncate(160) ?: site.metadata.description }}"/>

    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300..700&family=Work+Sans:wght@300..700&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>

    <link href="{{ url('theme://css/theme.css') }}?v={{ 'now'|date('U') }}" rel="stylesheet"/>

    {% block head %}{% endblock %}
</head>
<body class="bv-body{% block body_classes %} {{ page.header.body_classes }}{% endblock %}">
<script>
/* Auth modals (login / forgot / register): open one, close the others.
   Pass null to close all. Used by the header + the in-modal switch links so
   forgot/register open over the current page exactly like the login modal. */
function bvOpenOverlay(id){['bv-login-overlay','bv-forgot-overlay','bv-register-overlay'].forEach(function(o){var e=document.getElementById(o);if(e)e.classList.toggle('is-open',o===id);});document.body.style.overflow=id?'hidden':'';}
</script>

    {% block header %}
        {% include 'partials/navigation.html.twig' %}
    {% endblock %}

    <main class="bv-main blueprint-grid">
        {# Flash messages (login/email plugin notices: activation pending,
           activation confirmed, reset sent, form errors). Rendered site-wide
           so flashes redirected to `/` (WI-2) are visible inside the shell. #}
        {% block flash %}{% include 'partials/messages.html.twig' %}{% endblock %}
        {% block hero %}{% endblock %}
        {% block body %}
            {{ content|raw }}
        {% endblock %}
    </main>

    {% block footer %}
        {% include 'partials/footer.html.twig' %}
    {% endblock %}

    {# login_overlay is always included: the overlay's <form> is also the
       form rendered by Grav's /login route. Gating it would break /login
       under profiles where membership_signup is false. The visibility of
       the "Log ind" button itself is gated in navigation.html.twig. #}
    {% include 'partials/login_overlay.html.twig' %}
    {# Forgot/register modals open over the current page. Skip each on its own
       route, where the standalone page already renders that form (avoids a
       duplicate Form-plugin registration form on /opret-medlemskab). #}
    {% if page.template != 'forgot' %}
    {% include 'partials/forgot_overlay.html.twig' %}
    {% endif %}
    {% if feature_enabled('membership_signup') and page.template != 'register' %}
    {% include 'partials/register_overlay.html.twig' %}
    {% endif %}
    {% if feature_enabled('bug_report') %}
    {% include 'partials/bug_report_overlay.html.twig' %}
    {% endif %}
    {% if feature_enabled('feature_suggestion') %}
    {% include 'partials/feature_suggestion_overlay.html.twig' %}
    {% endif %}

    <script src="{{ url('theme://js/site.js') }}?v={{ 'now'|date('U') }}"></script>

    {% block bottom %}{% endblock %}
</body>
</html>
