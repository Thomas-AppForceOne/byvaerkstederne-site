{% extends 'partials/base.html.twig' %}

{# Edit-event page (/begivenheder/rediger/<key>) — the same inline card editor
   (partials/event_editor.html.twig), prefilled from the stored event
   (em_editor_state). The plugin has already resolved the object and enforced
   ownership; the hidden data[key] is a correlation value only (the POST handler
   re-resolves + re-authorizes against the STORED owner). #}

{% block body %}
    {% include 'partials/event_editor.html.twig' with {
        ee_action: '/begivenheder/rediger',
        ee_heading: 'Rediger begivenhed',
        ee_intro: 'Redigér din begivenhed direkte på kortet, som den ser ud i <a href="/vaerkstedskalenderen">værkstedskalenderen</a>.',
        ee_key: em_event_key,
        ee_submit_label: 'Gem ændringer'
    } %}
{% endblock %}
