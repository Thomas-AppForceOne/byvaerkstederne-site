# Composer-installed dev dependencies (phpunit + symfony/yaml for tests).
# Regenerated by `composer install`. Runtime deps, if any are added later,
# are force-committed under vendor/ explicitly.
/vendor/

# PHPUnit cache (speeds up local re-runs; not source).
/.phpunit.cache/
